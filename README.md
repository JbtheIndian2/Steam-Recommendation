# Steam-Recommendation
 A recommendation engine that suggests games based on **what a game is about** — its genres, community tags, and play modes — rather than on other users' behavior. Given a game, it returns its nearest neighbors in a learned tag-vector space.
